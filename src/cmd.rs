pub fn serve(port: u16, entry: &str) {
    println!("{}, {}", port, entry);
}

pub fn new_entry(entry: &str) {
    println!("{}", entry);
}

pub fn status(entry: &str) {
    println!("{}", entry);
}

pub fn build(entry: &str) {
    println!("{}", entry);
}
